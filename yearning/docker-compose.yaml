version: '3.7'
services:
  yearning:
    image: mritd/yearning
    container_name: yearning
    restart: on-failure
    ports:
      - "8000:8000"
    volumes:
      - "./conf.toml:/opt/yearning/conf.toml"
