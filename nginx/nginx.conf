# Generated by nginxconfig.io
# https://www.digitalocean.com/community/tools/nginx#?0.domain=mritd.me&0.document_root=%2F&0.redirect=false&0.cert_type=custom&0.ssl_certificate=%2Fetc%2Fnginx%2Fssl%2Fmritd.me.cer&0.php=false&0.proxy&0.proxy_pass=http:%2F%2Fhugo&0.root=false&0.access_log_domain&0.error_log_domain&ssl_profile=modern&server_tokens&brotli&log_not_found&user=nginx&pid=%2Fvar%2Frun%2Fnginx.pid&symlink=false

user nginx;
pid /var/run/nginx.pid;
worker_processes auto;
worker_rlimit_nofile 65535;

events {
	multi_accept on;
	worker_connections 65535;
}

http {
	charset utf-8;
	sendfile on;
	tcp_nopush on;
	tcp_nodelay on;
	types_hash_max_size 2048;
	client_max_body_size 16M;

	# MIME
	include mime.types;
	default_type application/octet-stream;

	# logging
	access_log /var/log/nginx/access.log;
	error_log /var/log/nginx/error.log warn;

	# SSL
	ssl_session_timeout 1d;
	ssl_session_cache shared:SSL:10m;
	ssl_session_tickets off;

	# OCSP Stapling
	ssl_stapling on;
	ssl_stapling_verify on;
	resolver 1.1.1.1 1.0.0.1 8.8.8.8 8.8.4.4 208.67.222.222 208.67.220.220 valid=60s;
	resolver_timeout 2s;

	# load configs
	include /etc/nginx/conf.d/*.conf;
}
